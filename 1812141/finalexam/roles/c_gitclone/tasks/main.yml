---
- name: git on centos
  yum:
    name: git
    state: latest

- name: clone on centos
  git:
    repo: '{{ prelim_exam}}'
    dest: ./prelim-exam
    version: prelim-exam
# tasks file for roles/c_gitclone
